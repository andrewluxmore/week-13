
# Week 13 – GitHub Pages Deployment (Instructor Solution)

- 404 fallback route implemented
- Accessible 'No results' state with clear/reset
- Vite `base` configured to `/week-13/`
- `gh-pages` scripts included. Deploy with: `npm i && npm run deploy`
